function botGetRandomInt() {
    return Math.floor(Math.random() * 100);
}
console.log(botGetRandomInt());
let game_active = true;
// Initializing DOM
let guessInput = document.querySelector('.guess');
while(game_active) {
    
}